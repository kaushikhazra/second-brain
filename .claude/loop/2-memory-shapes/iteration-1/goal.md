# 2 — every memory write lands in a shape

## Goal

> Every acceptance criterion in GitHub issue #2 holds against this repo's skills: an
> owner's brain has one shared shapes file and four memory skills, every memory write,
> change and removal in every skill routes through them, every write is verified by
> reading it back, and a check exists for each criterion that fails when the behaviour
> is removed.
